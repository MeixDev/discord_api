enum DiscordMessageNotificationsLevel {
  /// members will receive notifications for all messages by default
  allMessages,

  /// members will receive notifications only for message that @mention them by default
  onlyMentions,
}
